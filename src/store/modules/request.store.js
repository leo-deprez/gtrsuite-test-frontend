// import { sendGetRequest } from '@/services/api.service'

const state = {
  items: 2,
}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
}
